# P3 — Sửa Inter qua API · HOÀN THÀNH

**Thời điểm**: 2026-07-26 02:28
**Trạng thái**: ✅ ĐẠT toàn bộ Check

## Đã làm

### API (`OmrApiServer.java`)
| Method | Path | Hành động |
|---|---|---|
| DELETE | `/api/sheet/{n}/inter/{id}` | `RemovalTask` |
| POST | `/api/sheet/{n}/inter/{id}/role` | body `{"role":"Lyrics"}` → `SentenceRoleTask` |
| POST | `/api/book/undo` / `redo` | stack `UITask` trong server |
| POST | `/api/book/save` | `book.store()` |

Không đi qua `InterController`/`@UIThread` — gọi thẳng `UITask.performDo/Undo` (publish an toàn khi `entityService == null`).

### Viewer
- Toolbar: Undo / Redo / Save .omr (khi `· api`)
- Panel: dropdown **TextRole** (sentence) + nút **Xóa Inter**
- Toast trạng thái sau mỗi thao tác

## Check (đã chạy trên live server)

| Bước | Kết quả |
|---|---|
| Baseline 857 inters | ✅ |
| DELETE `#1269` (NOTEHEAD_BLACK grade 0.179) | ✅ còn 856, id biến mất |
| POST role `#3143` Direction → Lyrics ("Ballade") | ✅ `role=Lyrics`, Direction 36→35 |
| Undo ×2 | ✅ 1269 trở lại, role Direction |
| Redo ×2 + Save | ✅ `.omr` zip: **không còn id 1269**, **3143 role=Lyrics** |

Snapshot tĩnh đã chạy lại extract → 856 inters / 828 relations.

## Giới hạn

- Đổi role chỉ `setRole` (Capella gán nhãn). **Chưa** convert Word→LyricItem như `InterController.changeSentence` (cần cho export MusicXML lời đầy đủ — ghi vào P4/P5).
- Chưa assign shape / thêm Inter (P6).
- `.omr` trong `output/` đã bị sửa bởi Check (gitignore) — không ảnh hưởng git.

## Bàn giao P4

[plans/20260726_0228-p4-relation-va-role-sau.md](../plans/20260726_0228-p4-relation-va-role-sau.md)
