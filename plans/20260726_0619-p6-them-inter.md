# P6 — Thêm Inter (palette + click đặt) — ✅ XONG

**Ngày giờ:** 2026-07-26 06:19 · **Trạng thái:** XONG · Report: [../reports/20260726_0619-p6-them-inter.md](../reports/20260726_0619-p6-them-inter.md)

## Mục tiêu
Đối xứng với P3 (*xóa*): cho phép **thêm** ký hiệu/nốt từ web, click lên bản nhạc để đặt, tự gắn staff gần nhất + dò relation.

## Plan (đã thực hiện)
1. API `POST /api/sheet/{n}/inter` {shape,x,y[,w,h]} → `InterFactory.createManual` + `searchLinks` + `AdditionTask`. ✅
2. UI palette "+ Thêm" + chế độ click-đặt (crosshair, boxes bỏ pointer-events). ✅
3. Check: add nhiều loại shape, undo/redo, count, shape sai → 400, không đụng `.omr` đĩa. ✅

## Kết quả
Xem report. Book Yêu Xa vẫn 856 inters (test đã undo, không save).

## Bàn giao — việc còn lại (agent tiếp theo)
- **P4** Relation link/unlink (`LinkTask`/`UnlinkTask`) + convert Lyrics đầy đủ (không chỉ setRole).
- **P5** Export `.mxl` sau khi sửa → nạp lại vào alphaTab player để nghe.
- **P6b** Thêm text/lyrics (cần OCR glyph qua `InterController.addText`) + snap cao độ khi đặt nốt.
- **P7** Re-run step (BEAMS/RHYTHM…) sau khi sửa thủ công.

### Cách chạy để test
```bash
# Terminal 1: API server (build + load ~60–90s)
export JAVA_HOME=/opt/homebrew/opt/openjdk@25; export PATH="$JAVA_HOME/bin:$PATH"
export TESSDATA_PREFIX="$HOME/Library/Application Support/AudiverisLtd/audiveris/tessdata"
OMR="/Users/hacmieu/DevOps/HADE/audiveris/output/yeu-xa-sheet-nhac.omr"
./gradlew :app:run --no-daemon --console=plain \
  -PcmdLineArgs="-batch,-run,org.audiveris.omr.web.OmrApiServer,${OMR}"
# → chờ log "OmrApiServer listening on http://127.0.0.1:8080/api/"

# Terminal 2: web
cd web && npm run dev   # http://localhost:5173/ → tab "OMR Viewer"
```
