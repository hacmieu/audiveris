# Kế hoạch P3 — Sửa Inter qua API (+ bàn giao)

**Trạng thái**: ⬜ CHƯA BẮT ĐẦU
**Thời điểm**: 2026-07-26 02:21
**Tiền đề**: [P2 xong](../reports/20260726_0221-p2-omr-api-va-lop-capella.md)

---

## A. BÀN GIAO — agent mới đọc trước

### Đang ở đâu
Port GUI→web, chiến lược A. **P1 + P2 xong.** P3 = mutate.

### Đã có
| Path | Vai trò |
|---|---|
| `tools/omr_extract.py` | Extract tĩnh (+ role/value) |
| `web/src/OmrViewer.tsx` | Overlay + lớp Capella; ưu tiên `/api` |
| `app/.../web/OmrApiServer.java` | HttpServer đọc Book |
| `web/vite.config.ts` | proxy `/api` → :8080 |

### Chạy lại
```bash
# API (terminal 1)
export JAVA_HOME=/opt/homebrew/opt/openjdk@25; export PATH="$JAVA_HOME/bin:$PATH"
export TESSDATA_PREFIX="$HOME/Library/Application Support/AudiverisLtd/audiveris/tessdata"
./gradlew :app:run --no-daemon \
  -PcmdLineArgs="-batch,-run,org.audiveris.omr.web.OmrApiServer,/ABS/output/yeu-xa-sheet-nhac.omr"

# Web (terminal 2)
cd web && npm run dev   # http://localhost:5173 → OMR Viewer · phải thấy "· api"
```

### Sự thật
- 857 inters / 831 relations khớp id với extract.
- `TextRole` đã có; GUI dùng `SentenceRoleTask`. **Ưu tiên P3**: `POST` đổi role + remove Inter (false positive).
- `InterController` khóa `@UIThread` — P3 nên gọi thẳng `UITask.performDo` / `TaskHistory`, stub selection nếu cần.
- Bounds live có thể lệch 1–2px so với XML (stem) — đừng fail Check vì vậy.

---

## B. PDCA P3

### PLAN
Thêm endpoint ghi, tối thiểu:
1. `DELETE /api/sheet/{n}/inter/{id}` → `RemovalTask`
2. `POST /api/sheet/{n}/sentence/{id}/role` body `{"role":"Lyrics"}` → `SentenceRoleTask`
3. `POST /api/book/save` → `book.store()`
4. (Tuỳ chọn) `POST .../undo` `/redo`

### CHECK
- [ ] Xóa `NOTEHEAD_BLACK` grade 0.179 → reload inters không còn id đó
- [ ] Đổi sentence Direction → Lyrics → `role` trong JSON đổi
- [ ] Save → mở lại `.omr` (GUI hoặc extract) vẫn giữ
- [ ] Undo khôi phục

### Không làm P3
Palette / add Inter (P6), re-run step (P7), export (P5).
