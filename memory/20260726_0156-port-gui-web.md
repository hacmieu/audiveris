# Bản ghi quyết định: Port Audiveris GUI → Web

**Thời điểm**: 2026-07-26 01:56

## Quyết định

- **Chiến lược A**: web UI mỏng (React trong `web/`) + JVM edit-service bọc `Book`/`SIGraph`/`UITask`/`ScoreExporter`. Không compile Java→web, không viết lại editor thuần JS.
- Chia **7 phase PDCA**, duyệt sau mỗi phase. Bắt đầu **P1 = overlay read-only**, chưa có server.

## Sự thật kỹ thuật cần nhớ

- `.omr` = ZIP: `book.xml` + `sheet#N/sheet#N.xml` (JAXB) + `sheet#N/BINARY.png`.
- Inter có `shape`, `grade`, `id`, `<bounds x y w h>`; sheet#1 có ~857 bounds, 831 relations.
- Engine headless (batch) OK. Editor = ~289 file Swing, `InterController` khóa `@UIThread`, không REST.
- Điểm bám server tương lai: `CLI -run <RunClass extends RunClass>`.

## Trạng thái

PLAN xong, đã push. Chờ duyệt để build P1.

Chi tiết: [../plans/20260726_0156-port-gui-web.md](../plans/20260726_0156-port-gui-web.md) · [../reports/20260726_0156-port-gui-web.md](../reports/20260726_0156-port-gui-web.md)
